name: Run Custom CodeQL Query on Kotlin

on:
  push:

  pull_request:


jobs:
  codeql-custom-query:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 1: Initialize CodeQL CLI only (no queries yet)
      - name: Initialize CodeQL CLI only
        uses: github/codeql-action/init@v3
        with:
          languages: kotlin

      # Step 2: Install official Kotlin pack globally (CLI now available)
      - name: Install official Kotlin pack globall
